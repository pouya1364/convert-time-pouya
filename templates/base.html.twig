<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('asset/styles/bootstrap/bootstrap.min.css') }}">
            <!-- Bootstrap date Picker -->
            <link rel="stylesheet" href="{{ asset('asset/styles/bootstrap/bootstrap-datepicker3.css') }}">
            <!-- Bootstrap time Picker -->
            <link rel="stylesheet" href="{{ asset('asset/styles/bootstrap/bootstrap-timepicker.css') }}">
        {% endblock %}

    </head>
    <body>
        {% block body %}
        
            {% block javascripts %}
                    <!-- jQuery first, then Bootstrap JS -->
                    <script src={{ asset('asset/js/jquery/jquery.min.js') }}></script>
                    {# <script src={{ asset('asset/js/bootstrap/bootstrap.min.js') }}></script>                    #}
                    <!-- Include Time and Date Range Picker -->
                    {# <script src={{ asset('asset/js/bootstrap/bootstrap-datepicker.min.js') }}></script>                    #}
                    {# <script src={{ asset('asset/js/bootstrap/bootstrap-timepicker.min.js') }}></script>                    #}
            {% endblock %}

        {% endblock %}
    </body>
</html>
